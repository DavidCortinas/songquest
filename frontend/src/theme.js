import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#013a57',
    },
    secondary: {
      main: '#007fbf',
    },
    tertiary: {
      main: '#eff3f5',
    },
  },
});

export default theme;
